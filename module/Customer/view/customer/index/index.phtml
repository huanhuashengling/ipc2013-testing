<?php
/**
 * Unit-Testing einer Zend Framework 2 Anwendung
 *
 * Zend Framework Session auf der International PHP Conference 2013 in München
 *
 * @package    Customer
 * @author     Ralf Eggert <r.eggert@travello.de>
 * @copyright  Ralf Eggert <r.eggert@travello.de>
 * @link       http://www.ralfeggert.de/
 */

/**
 * namespace definition and usage
 */

use Customer\Entity\CustomerEntity;

/**
 * Index view script
 *
 * @package    Customer
 */

?>
<table class="table table-striped table-bordered table-hover">
    <thead>
    <tr>
        <th>ID</th>
        <th>First Name</th>
        <th>Last Name</th>
        <th>Address</th>
        <th>Country</th>
        <th style="text-align: right;"><a href="<?php echo $this->url('customer/action', array('action' => 'create'));?>" class="btn btn-small"><i class="icon-plus"></i> create</a></th>
    </tr>
    </thead>
    <tbody>
    <?php foreach ($this->customerList as $customerEntity) : /* @var $customerEntity CustomerEntity */ ?>
        <tr>
            <td><?php echo $customerEntity->getId(); ?></td>
            <td><?php echo $customerEntity->getFirstname(); ?></td>
            <td><?php echo $customerEntity->getLastname(); ?></td>
            <td><?php echo $customerEntity->getStreet() . ', ' . $customerEntity->getPostcode() . ' ' . $customerEntity->getCity(); ?></td>
            <td><?php echo $customerEntity->getCountry(); ?></td>
            <td style="text-align: right;">
                <a href="<?php echo $this->url('customer/action', array('action' => 'show', 'id' => $customerEntity->getId()));?>" class="btn btn-small"><i class="icon-search"></i> show</a>
                <a href="<?php echo $this->url('customer/action', array('action' => 'update', 'id' => $customerEntity->getId()));?>" class="btn btn-small"><i class="icon-pencil"></i> edit</a>
                <a href="<?php echo $this->url('customer/action', array('action' => 'delete', 'id' => $customerEntity->getId()));?>" class="btn btn-small" onClick="return confirm('Are You Sure?')"><i class="icon-trash"></i> delete</a>
            </td>
        </tr>
    <?php endforeach; ?>
    </tbody>
</table>
<hr />
<a href="<?php echo $this->url('home'); ?>" class="btn btn-success">Overview</a>

